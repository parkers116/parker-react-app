import React, { Component } from 'react'

export default class NotMatch extends Component {
    render() {
        return (
            <React.Fragment>
                <h2>Not Match</h2>
            </React.Fragment>
        )
    }
}
